% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import_data.R
\name{import_data}
\alias{import_data}
\title{Import data}
\usage{
import_data(
  x,
  var_names,
  var_types,
  elic_types,
  ...,
  name = "Elicitation Dataset",
  sep = ","
)
}
\arguments{
\item{x}{either a \code{\link[base:data.frame]{data.frame}}, a string with the path to
a \code{csv} or \code{xlsx} file, or anything accepted by the
\link[googlesheets4:range_read]{read_sheet()} function.}

\item{var_names}{character vector with the name of the estimated variables,
used only when \code{x} is a Google Sheets file.}

\item{var_types}{character vector with short codes indicating the variable
type, used only when \code{x} is a Google Sheets file. If only one \code{var_type} is
provided, its value is recycled for all variables. See Variable Types for
more.}

\item{elic_types}{character vector with short codes indicating the
elicitation type, used only when \code{x} is a Google Sheets file. If only one
\code{elic_type} is provided, its value is recycled for all variables. See Data
Format and Elicitation Types for more.}

\item{...}{Unused arguments, included only for future extensions of the
function.}

\item{name}{character, used to bind a name to the dataset.}

\item{sep}{character used as field separator, used only when \code{x} is a \code{csv}
file.}
}
\value{
An object of class \code{elicitr}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}} \code{load_data()} imports data from
different sources and format them for further analysis.
}
\section{Data Format}{


Data are expected to have a unique identifier always as first column and
named \code{id} lower case. After the unique identifier it is expected one or more
blocks which follow the specifications below:

One point elicitation:
\itemize{
\item \code{var_best}: best estimate for the variable
}

Three points elicitation:
\itemize{
\item \code{var_min}: minimum estimate for the variable
\item \code{var_max}: maximum estimate for the variable
\item \code{var_best}: best estimate for the variable
}

Four points elicitation:
\itemize{
\item \code{var_min}: minimum estimate for the variable
\item \code{var_max}: maximum estimate for the variable
\item \code{var_best}: best estimate for the variable
\item \code{var_conf}: confidence for the estimate
}

Column \code{id} is unique, the other columns are a block and can be repeated for
each variable.
}

\section{Variable Types}{


Variable types must be provided in a single string containing short codes,
e.g. "pPN".

Valid short codes are:
\itemize{
\item \code{Z}: \emph{integers}, when the estimate must be an integer number in the
interval (-Inf, Inf).
\item \code{N}: \emph{positive integers}, when the estimate must be an integer number in
the interval (0, Inf).
\item \code{z}: \emph{negative integers}, when the estimate must be an integer number in
the interval (-Inf, 0].
\item \code{R}: \emph{reals}, when the estimate must be a real number in the interval
(-Inf, Inf).
\item \code{s}: \emph{positive reals}, when the estimate must be a real number in the
interval (0, Inf).
\item \code{r}: \emph{negative reals}, when the estimate must be a real number in the
interval (-Inf, ].
\item \code{p}: \emph{probability}, when the estimate must be a real number in the interval
(0, 1).
}
}

\section{Elicitation types}{


Elicitation types must be provided in a single string containing short codes,
e.g. "134".

Valid short codes are:
\itemize{
\item \code{1}: \emph{one point elicitation}, when only the best estimate is provided.
\item \code{3}: \emph{three points elicitation}, when the minimum, maximum, and best
estimates are provided.
\item \code{4}: \emph{four points elicitation}, when the minimum, maximum, best, and
confidence estimates are provided.
}
}

\author{
Sergio Vignali
}
